#if 0
#include <stdio.h>
//(数据类型)标识符命名规则 只能由字母、数字、下划线命名

int main() {
	//常量
	int weight = 5;
	int price = 2;
	int sum = weight * price;
	//占位符%d表示输出一个整型数据
	printf("%d", sum);


	scanf("%d", &a);
	printf("十进制：%d", a);
	printf("八进制，%o", a);
	printf("十六进制，%X\n", a);
	return 0;
}
#endif